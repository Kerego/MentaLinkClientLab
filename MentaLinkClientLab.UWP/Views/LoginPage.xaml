<Page
    x:Class="MentaLinkClientLab.UWP.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MentaLinkClientLab.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Background="Wheat">
		<TextBox Name="HostBox" PlaceholderText="Host" HorizontalAlignment="Center" MinWidth="140" VerticalAlignment="Center" Text="{x:Bind viewModel.Host, Mode=TwoWay}"></TextBox>
		<TextBox Name="UserBox" PlaceholderText="User" HorizontalAlignment="Center" MinWidth="140" VerticalAlignment="Center" Text="{Binding User, Mode=TwoWay}"></TextBox>
		<Button Command="{Binding ConnectCommand}" Content="Connect" VerticalAlignment="Center"></Button>
		<TextBlock Name="ConnectLabel" Text="{Binding Info}"></TextBlock>
	</StackPanel>
</Page>
